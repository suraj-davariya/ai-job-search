{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CareerForge Scam-Pattern Catalog",
  "description": "Community-extensible, locale-aware red-flag signals for posting-legitimacy assessment (REQ-8002, REQ-8005).",
  "type": "object",
  "required": ["globalSignals"],
  "properties": {
    "globalSignals": {
      "type": "array",
      "items": { "$ref": "#/$defs/signal" },
      "description": "Signals that apply in every market"
    },
    "byRegion": {
      "type": "object",
      "description": "Market-specific signals keyed by locale-pack code (e.g. 'IN', 'US'). Communities add regional scams here without touching core.",
      "additionalProperties": {
        "type": "array",
        "items": { "$ref": "#/$defs/signal" }
      }
    }
  },
  "$defs": {
    "signal": {
      "type": "object",
      "required": ["id", "label", "severity", "description"],
      "properties": {
        "id": { "type": "string" },
        "label": { "type": "string" },
        "severity": { "type": "string", "enum": ["high", "medium", "low"] },
        "description": { "type": "string" },
        "examples": { "type": "array", "items": { "type": "string" } }
      }
    }
  }
}
