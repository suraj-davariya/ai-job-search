# Epic 12 — v1.2 (Interview Prep + Polish) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Ship the final planned milestone — `/reset`, interview-prep gap-closure, an
ADR-0004 portal-adapter example, a research agent, and an E2E consistency pass — leaving
all four milestones complete. Design:
[`../specs/2026-06-17-epic-12-v1.2-polish-design.md`](../specs/2026-06-17-epic-12-v1.2-polish-design.md).

**Architecture:** Prompt-as-code (ARCH-0001) for commands/agent/adapter SKILL.md; real
Bun/TypeScript for the adapter `cli/` (ADR-0004). Verification is grep/trace + a smoke run.

**Out of scope:** real market adapters, dashboard/demo changes, rewriting the interview
framework.

---

### Task 1: `/reset` full command (T-101, REQ-0080–0085)

**Files:** Replace `.claude/commands/reset.md`.

- [ ] **Step 1 — Frontmatter:** `description`, `argument-hint: "[profile | documents | all]"`, `allowed-tools: Read, Edit, Write, Glob, Grep, Bash`.
- [ ] **Step 2 — Hard invariants:** inventory-before-delete, exact-`RESET` confirmation, framework-preservation, no-fabrication, human-in-the-loop, "cannot be undone".
- [ ] **Step 3 — Stages:** scope selection (REQ-0080), pre-reset inventory + explicit never-touch list (REQ-0081), exact-`RESET` confirm (REQ-0082), profile reset execution preserving framework (REQ-0083), documents reset preserving structure + README (REQ-0084), post-reset guidance (REQ-0085).
- [ ] **Step 4 — Never-touch list:** `03-writing-style.md`, `04-job-evaluation.md` framework, `05` LaTeX structure, `06-cover-letter-templates.md`, `07` framework (only STAR examples clear), `documents/README.md`.
- [ ] **Step 5 — REQ→stage trace** table.
- [ ] **Step 6 — Verify:** `grep -noE 'REQ-008[0-5]' .claude/commands/reset.md` → all six; `grep -niE 'RESET|cannot be undone|never (touch|delete)|framework' reset.md` → present.
- [ ] **Step 7 — Commit** `feat(reset): implement /reset profile/documents reset (Epic 12, T-101)`.

### Task 2: Interview-prep gap closure (T-100, REQ-3051/3052/3054)

**Files:** Edit `.claude/skills/job-application-assistant/07-interview-prep.md`.

- [ ] **Step 1** — Add a **"You don't have X"** tough-question entry (REQ-3051): answer the
  honest-reframing way — acknowledge the gap, bridge from adjacent experience, show fast-
  learning evidence; never claim the missing skill.
- [ ] **Step 2** — Bring each "Questions to Ask" category (Role/Team/Tech/Culture) to
  **3–5** questions (REQ-3052); keep culture questions aimed at preventing post-hire
  disappointment.
- [ ] **Step 3** — Add to Follow-Up Etiquette (REQ-3054): don't call to "stand out" (risks
  a negative impression); a brief follow-up is acceptable after **2+ weeks** with no stated
  timeline.
- [ ] **Step 4 — Verify:** `grep -niE "don't have|stand out|2\+ weeks" 07-interview-prep.md` → present; each Q-category has ≥3 bullets.
- [ ] **Step 5 — Commit** `feat(interview): close REQ-3051/3052/3054 interview-prep gaps (Epic 12, T-100)`.

### Task 3: Portal adapter pattern example (T-102, ADR-0004)

**Files:** Create `.agents/skills/example-portal/{SKILL.md, cli/package.json, cli/index.ts, url-reference.md}`.

- [ ] **Step 1 — `SKILL.md`:** trigger keywords, the input→output contract, how the
  `job-scraper` skill invokes the `cli/`, and a bold "TEMPLATE — copy this directory to add
  a portal" banner. Note `.agents/skills/` is the Plane-2 location (not auto-loaded).
- [ ] **Step 2 — `cli/package.json`:** name, `type: module`, a `start`/bin entry, Bun
  runtime note; zero or minimal deps.
- [ ] **Step 3 — `cli/index.ts`:** implements the ADR-0004 contract — parse
  keywords/location/date-range args, emit a JSON array of `{title, company, location, url,
  date, snippet}`, exit 0 on success / 1 on error. Returns **clearly-labelled example
  data** (no real scraping) so `bun run`/`node` works as a teaching tool.
- [ ] **Step 4 — `url-reference.md`:** how to find a portal's search URL patterns.
- [ ] **Step 5 — Smoke run (if runtime present):** `bun run cli/index.ts --keywords "data engineer" --location "Remote"` (or `npx tsx`/`node` fallback) → valid JSON, exit 0.
- [ ] **Step 6 — Reference it** from `docs/development/contribution-workflow.md` (or implementation-guide-job-search.md): "Adding a job portal → copy `.agents/skills/example-portal/`".
- [ ] **Step 7 — Verify:** `grep -n 'example-portal' docs/development/*.md` → present; adapter dir matches the ADR-0004 contract file set.
- [ ] **Step 8 — Commit** `feat(adapters): add ADR-0004 example portal adapter + contributor guide (Epic 12, T-102)`.

### Task 4: Research agent definition (T-103)

**Files:** Create `.claude/agents/research-agent.md`.

- [ ] **Step 1 — Frontmatter:** `name: research-agent`; `description` (fresh-context
  company/market/topic research, sourced, content-only); `tools: Read, WebSearch, WebFetch`.
- [ ] **Step 2 — Body** (mirror reviewer.md structure): role + isolation rationale; inputs
  (the research brief, inline); hard constraints (no fabrication, cite every URL, returns
  research not candidate facts, does not edit files); a structured output shape (findings
  grouped by theme, each with source URL + confidence).
- [ ] **Step 3 — Verify:** `grep -niE 'fabricat|source|WebSearch|do not edit' .claude/agents/research-agent.md` → present.
- [ ] **Step 4 — Commit** `feat(agents): add research-agent definition (Epic 12, T-103)`.

### Task 5: Docs reconcile + E2E consistency pass (T-104)

**Files:** `README.md`, `docs-site/content/docs/commands/reset.mdx`, `docs-site/content/docs/commands/meta.json`.

- [ ] **Step 1 — README:** add a `### /reset` "Commands in depth" section; remove `/reset`
  from the Planned table (table now empty → replace with a "all planned commands shipped"
  line or drop the section); flip the **v1.2 roadmap row to ✅ Complete**; update the
  Commands badge (+`/reset`) and the tree note (drop "stub, v1.2").
- [ ] **Step 2 — docs-site:** new `commands/reset.mdx` (mentor voice, stress the safety:
  inventory, type `RESET`, framework preserved); add `"reset"` to `meta.json` pages.
- [ ] **Step 3 — E2E traceability sweep:** grep that every v1.1/v1.2 REQ id is referenced
  by its artifact; confirm no remaining `stub` labels for shipped commands; confirm
  `docs-site` vitest green.
- [ ] **Step 4 — Commit** `docs(v1.2): document /reset, mark v1.2 complete + E2E consistency pass (Epic 12, T-104)`.

---

## Self-Review

- **Spec coverage:** REQ-0080–0085→Task 1 · REQ-3051/3052/3054→Task 2 · ADR-0004→Task 3 ·
  research agent→Task 4 · roadmap/E2E→Task 5.
- **Naming consistency:** `/reset` preserve-list matches `/setup`+`/expand` write targets;
  adapter contract matches ADR-0004 exactly; agent format matches reviewer.md.
- **Safety:** `/reset` never wired to the dashboard console; destructive path gated by
  inventory + exact-string confirm.
- **No placeholders:** each task names exact files + concrete verification.

## Out of scope

Real market adapters, dashboard/demo changes, interview-framework rewrite.
