# Epic 10 — `/expand` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship `/expand` competency expansion (REQ-0050–0055, ARCH-0010) and heal the
dashboard's `/expand` console link. Full command in the `setup.md` style — additive-only,
no new skill.

**Architecture:** Prompt-as-code (ARCH-0001). Profile Manager operation (ARCH-0010,
Competency Expander). No build step — verification is grep/trace checks. Design:
[`../specs/2026-06-17-epic-10-expand-design.md`](../specs/2026-06-17-epic-10-expand-design.md).

**Out of scope:** `/reset` (Epic 12), interview roleplay (Epic 12), `/setup` changes,
dashboard/demo-domain changes.

---

### Task 1: `/expand` full command

**Files:**
- Replace: `.claude/commands/expand.md` (currently a stub)
- Canonical: REQ-0050–0055, ARCH-0010, business-rules §7.1/§7.3

- [ ] **Step 1 — Frontmatter:** `description`; `argument-hint` (none / optional source
  hint); `allowed-tools: Read, Edit, Write, Glob, Grep, Bash, WebSearch, WebFetch`
  (WebFetch for GitHub/URLs; WebSearch for enrichment).
- [ ] **Step 2 — Hard invariants block** (mirror setup.md): read-before-write,
  **additive-only** (never modify/remove), idempotency via source annotations,
  no-fabrication (ARCH-0007), no-writes-without-confirmation (REQ-0053), human-in-the-loop.
- [ ] **Step 3 — Stages, one per REQ:** source scan order (REQ-0050), web enrichment
  direct+inference (REQ-0051), competency map by category with source tracing (REQ-0052),
  confirmation options all/review/skip (REQ-0053), additive writes with source
  annotations + inferred labels (REQ-0054), expansion report (REQ-0055).
- [ ] **Step 4 — Target-files table:** additive targets `01`, `02` (Inferred Items), `04`;
  explicit never-touch list (`03`, `05`, `06`, `07`, `search-queries.md`).
- [ ] **Step 5 — REQ→stage trace** table at the bottom (setup.md style).
- [ ] **Step 6 — Verify REQ coverage:** `grep -noE 'REQ-005[0-5]' .claude/commands/expand.md` → all six present.
- [ ] **Step 7 — Verify invariants:** `grep -niE 'additive|never (modify|remove)|ARCH-0007|source annotation' expand.md` → present.
- [ ] **Step 8 — Verify dashboard link resolves (confirm only, no change):** `grep -n 'expand' dashboard/lib/run/allowlist.ts`.
- [ ] **Step 9 — Commit** `feat(expand): implement /expand competency expansion (Epic 10, T-080–T-084)`.

### Task 2: README — promote `/expand` to shipped

**Files:**
- Modify: `README.md`

- [ ] **Step 1** — Add a `### /expand` "Commands in depth" section (what it scans,
  additive-only, source annotations, the report).
- [ ] **Step 2** — Remove the `/expand` row from the "Planned commands" table; flip the
  v1.1 Expand roadmap row to ✅ Complete (so all of v1.1 is now complete); update the
  Commands badge to include `/expand`; update the directory-tree note (drop "stub, v1.1").
- [ ] **Step 3 — Verify:** `grep -n '/expand' README.md` shows it in the commands section, not the planned table.
- [ ] **Step 4 — Commit** `docs(readme): document /expand, mark v1.1 complete (Epic 10)`.

### Task 3: docs-site command page + nav

**Files:**
- Create: `docs-site/content/docs/commands/expand.mdx`
- Modify: `docs-site/content/docs/commands/meta.json`

- [ ] **Step 1** — Write `expand.mdx` in the mentor voice of the other command pages:
  what it scans, the competency map + confirmation, additive-only with source notes, the
  report, and the dashboard tie-in. No self-congratulatory copy.
- [ ] **Step 2** — Add `"expand"` to `meta.json` `pages` (after `upskill`).
- [ ] **Step 3 — Verify:** run `vitest run` in `docs-site/` → green.
- [ ] **Step 4 — Commit** `docs(docs-site): add /expand command page + nav (Epic 10)`.

---

## Self-Review

- **Spec coverage:** REQ-0050→scan · 0051→enrichment · 0052→map · 0053→confirmation ·
  0054→additive writes · 0055→report. All in Task 1.
- **Naming consistency:** target files + never-touch list match setup.md and the design;
  source-annotation format `*(Source — Item)*` identical to REQ-0054.
- **Dashboard:** no code change; Task 1 Step 8 confirms the allowlist entry resolves.
- **No placeholders:** each task names exact files + concrete verification.

## Out of scope

`/reset` & interview roleplay (Epic 12), `/setup` changes, dashboard/demo-domain changes.
